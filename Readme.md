1. Classes must be dependent through interfaces not on implementation
2. Never return an entity from service class, entities must not be exposed 
3. Use proper naming follow java naming convention for methods, classes,fields and etc.
4. Follow packaging structure of the project, each services must have an interface in services folder, the implementation details must be in a package dedicated to that service. Don't put everything in dto's package it will become a dumping graund.